"""
psplice — GPU-native CLI for runtime transformer surgery.

Load a foundation model onto your GPU and modify its behavior at inference
time: activation steering, head masking, layer skipping, LoRA hot injection,
and decoding controls — all without touching model weights.
"""

__version__ = "0.1.0"
